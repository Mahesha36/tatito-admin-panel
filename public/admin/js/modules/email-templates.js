'use strict';
/* Email Templates — page module (design-only)
   Wired automatically by admin-design.js when email-templates.html loads.
   Shared runtime already handles: modals, tabs, toggles, toasts,
   confirms, dropdowns, sidebar, topbar. Add page-specific behavior here. */
Design.register('email-templates', function (Design) {
    /* design-mode: nothing page-specific beyond the shared runtime yet */
});
